#include<stdio.h>
#include<conio.h>
void main()
{
int i,a,b;
printf("\nenter the starting limit :");
scanf("%d",&a);
printf("\nenter the ending limit:");
scanf("%d",&b);
for(i=a;i<=b;i++)
{
if(i%2==1)
{
printf("\n odd numbers are:%d",i);
}}
getch();}
